.git/
.claude/
.codegraph/
apm_modules/
graphify-out/

# Gauge run output. Root-owned from inside the container, so leaving it in the
# build context makes `docker build` fail on the host.
.gauge/
reports/
logs/
