# language: ja
機能: ステップが子ワークフローの saga

  **ステップがアクティビティでない形、その2。** approval が「待つ」だけだったのに対し、
  ここでは実行の手段そのものが変わり、しかも forward と取り消しで別々になる。

  ステップはアクティビティに限らない。しかも forward と取り消しで別々に選べる。ここでは
  梱包を子ワークフローで実行し、**その取り消しはアクティビティ1本**にしている。予約と配送は
  アクティビティのまま。

  見たいのは2つ。実行の仕方が混ざっても巻き戻しは1つの逆順で回ること。そして**実行の仕方が
  違っても、forward と取り消しが同じ冪等キーを受け取る**こと。キーはワークフローが
  1回導出し、子にもアクティビティにもリクエストに入れて渡す。ライブラリは
  関与しない。

  シナリオ: 子ワークフローで実行し、アクティビティで取り消す

    配送で失敗させる。梱包の取り消しは子ワークフローとして起動されるはず。

    前提 子ワークフローを含む注文 "mixed" を "ship" で失敗させる
    ならば saga は "no carrier available" で失敗する
    かつ 子ワークフロー "pack" が起動された
    かつ ステップ "reserve, ship, ship:undo, pack:undo, reserve:undo" が実行された
    かつ 梱包と取り消しが見た冪等キーは一致する
    かつ 注文は "reserve, ship" を保持していない

  シナリオ: 正常系では取り消しは起動されない
    前提 子ワークフローを含む注文 "packed"
    ならば saga は成功する
    かつ 子ワークフロー "pack" が起動された
    かつ ステップ "reserve, ship" が実行された
